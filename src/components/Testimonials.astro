---
import Icon from './Icon.astro';
import { testimonials } from '../data/testimonials';
import { site } from '../data/site';
---
<section class="section bg-subtle/50">
  <div class="container-x">
    <div class="mx-auto max-w-2xl text-center">
      <span class="eyebrow">Loved by UK viewers</span>
      <h2 class="mt-4 text-3xl sm:text-4xl md:text-5xl font-bold">{site.reviews.rating} / 5 from {site.reviews.count}+ customers</h2>
      <p class="mt-4 text-text-muted text-sm sm:text-base">Real customers from London to Glasgow. Setup takes minutes, support responds in under two.</p>
    </div>

    <div class="slider mt-10 md:mt-14 md:grid md:grid-cols-2 lg:grid-cols-3 md:gap-5">
      {testimonials.map((t) => (
        <figure class="card p-5 sm:p-6 w-[85%] sm:w-[55%] md:w-auto">
          <div class="flex items-center gap-1 text-accent">
            {Array.from({ length: t.rating }).map(() => <Icon name="star" size={14} />)}
          </div>
          <blockquote class="mt-4 text-sm text-text-primary leading-relaxed">“{t.body}”</blockquote>
          <figcaption class="mt-5 flex items-center gap-3 text-xs text-text-muted">
            <span class="flex h-9 w-9 items-center justify-center rounded-full bg-grad-brand font-semibold text-white">
              {t.name.charAt(0)}
            </span>
            <span>
              <span class="block font-semibold text-text-primary">{t.name}</span>
              <span>{t.city}, UK</span>
            </span>
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>
